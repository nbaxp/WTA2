@using WTA.Application.Extensions
@inject IStringLocalizer localizer
@{
    var action = Url.Action();
}
@section scripts{
    <template id="page">
        <h1>Home</h1>
    </template>
    <script type="module">
        window.page = {
            template: '#page',
            setup() {
                return {
                }
            }
        };
    </script>
}
